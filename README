Note: The CUDA portion of this code does not work as intended in its current state. Caveat emptor.


To begin development:
- git clone <your ssh url>
- sudo <package manager, commonly apt-get> install nvidia-cuda-toolkit

To compile:
- make

To run:
- ./cudahsh.out input output

To debug:
- gdb ./cudahsh
- run input output
- backtrace
